# NEXUS Backend Requirements
# Core dependencies for the data fetching pipeline

# HTTP Requests
requests>=2.28.0

# Environment Variables
python-dotenv>=1.0.0

# RSS Feed Parsing
feedparser>=6.0.0

# Google API (Optional - for Google CSE search)
google-api-python-client>=2.0.0
google-auth>=2.0.0

# Data Processing
# (Standard library: json, datetime, pathlib, typing, concurrent.futures)
